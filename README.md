This repository is a Spring Boot App that consumes the APIS from http://deckofcardsapi.com/.

To build this project, 
run the following :
-- mvn clean install

To run the tests:
-- mvn clean verify

To run the application:
-- mvn spring-boot:run

